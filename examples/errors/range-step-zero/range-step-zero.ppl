// Runtime error (type-checks): range_step with a zero step.

template RangeStepZero {
  variables {
    step: int
  }

  prompt {
    {% for i in range_step(0, 5, step) %}
    {{ i }}
    {% end %}
  }
}
