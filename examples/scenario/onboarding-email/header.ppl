// An included sub-template. Its prompt body is dedented (§8.4) before it is
// spliced into the parent at the include's (dedented) column.

template Header {
  variables {
    company: string
  }

  prompt {
Welcome to {{ company }}!
  }
}
