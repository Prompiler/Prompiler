// Demonstrates `func` declarations: reassignable local `var`, nested
// `if`/`else`, a `for` loop with early `return`, pass-by-value arguments,
// and a forward reference (a function used before its definition).

func sign(n: int): string {
  if (n < 0) {
    return "negative"
  } else {
    if (n == 0) {
      return "zero"
    } else {
      return "positive"
    }
  }
}

func contains(xs: string[], target: string): bool {
  for (x in xs) {
    if (x == target) {
      return true
    }
  }
  return false
}

func add(a: int, b: int): int {
  var total = a + b
  return total
}

func bump(x: int): int {
  var y = x
  y = y + 1
  return y
}

template Functions {
  variables {
    n: int
    items: string[]
    needle: string
  }

  prompt {
    Sign: {{ sign(n) }}
    Contains: {{ contains(items, needle) }}
    Sum: {{ add(n, 10) }}
    Bump: {{ n }} -> {{ bump(n) }} -> {{ n }}
    Forward: {{ later(n) }}
  }
}

func later(x: int): int {
  return x * 2
}
